# Cost ledger — what the local layer saved (be seen to be fair)

> The same transparency the dice and the world-tick follow, applied to tokens.
> The engine spends Claude on live play and player-facing prose; the local layer
> (on your GPU) handles retrieval, and routine world-moves are templated
> deterministically for free. Log it so the cost split is visible, not a vibe.
> Append newest at the bottom; stamp the day.
>
> Columns: **Movers templated** = routine moves written for free by
> `world_scribe.py`. **Beats → Sonnet** = collisions/reflection run on
> `world-director-lite`. **Beats → Opus** = secret-bearing pivots run on
> `world-director`. **Chapters** = prose authored by Claude (`chapter-renderer`).

| Day | Movers templated (free) | Beats → Sonnet (lite) | Beats → Opus (director) | Chapters (Claude) | Notes |
|-----|-------------------------|-----------------------|-------------------------|-------------------|-------|
| —   | —                       | —                     | —                       | —                 | (none yet) |
